let a = 1
let b = 1
DECIMAL = a + b
BYNARY = (a + b).toString(2)
console.log(DECIMAL + " in decinmal or "+ BYNARY +" in binary");

/*

function op(s, v1Number, v2Number) {
    var result = 0;
    switch (s) {
    case '+':
        result = v1Number + v2Number;
        break;
    case '-':
        result = v1Number - v2Number;
        break;
    case '*':
        result = v1Number * v2Number;
        break;
    case '/':
        result = v1Number / v2Number;
        break;
    case '%':
        result = v1Number % v2Number;
        break;
    }
    console.log(result.toString(2));
}

op('+', 1, 12);
op('+', 1, 3); //note que funciona igual

*/